import React from "react";
import c from "./Settings.module.css"

const Settings = (props) => {
    return(
        <div className={c}>
            Settings
        </div>
    )
};

export default Settings;